---
id: pipeline-shorts
title: 이 저장소 영상 파이프라인 소개 쇼츠
date: 2026-08-08
status: draft
variants: [9x16]
duration: 30s
voice: Azure / ko-KR-InJoonNeural (+9%)
scenes: tools/video/scenes.pipeline.json
score: 3.3
---

## 제작

이 저장소의 영상 파이프라인 자체를 소개하는 쇼츠. **대본만 쓰면 목소리·자막·모션이 붙는다**는
한 문장을 30초에 담는다. 찍을 앱 화면이 없으므로 `scenes: []` 로 두고 타이포 카드와 B롤만 쓴다.

**컨셉을 대본보다 먼저 검증했다.** `video-concept` 워크플로(에이전트 13개, 503,301 토큰, 11.8분)로
3안을 병렬 생성해 세 관점(제작가능성·후킹·사실검증)으로 심사했다. 확정안은 "0개, 그리고".

훅의 숫자를 **355에서 0으로 바꾼 것**이 이 영상의 핵심 결정이다. 후킹 심사의 지적이 정확했다 —
355자는 비교 기준 없이는 많은지 적은지 판별할 수 없어 "격차"가 성립하지 않는다. 반면 0은 참조
대상이 필요 없고, 결론을 닫는 대신 "그럼 뭘로 만들었나"라는 구멍을 연다.

내레이션에 나오는 숫자는 **0(녹화한 화면 수)과 7(모션 템플릿 수)** 둘뿐이고 양쪽 다 실측이다.
검증에서 "18.4분 / 분당 353자" 같은 수치가 어떤 조합으로도 재현되지 않는다는 지적이 나와
그 블록을 통째로 뺐다. **그런데 화면에는 세 번째 숫자가 나왔다** — 아래 "걸린 것" 참조.

목소리는 한강 쇼츠와 같은 InJoon(Azure)으로 맞췄다.

## 소재

| 파일 | 출처 | 라이선스 | 쓴 곳 |
|---|---|---|---|
| `broll/ai-create.mp4` | Mixkit (골드 파티클) | Mixkit Free License | proof 구간, shade 0.4 |
| `broll/ai-datacenter.mp4` | Mixkit (서버룸 복도) | Mixkit Free License | code 구간, shade 0.45 |
| `bgm/mixkit-471.mp3` | Mixkit (98.77초) | Mixkit Free License | 전체, gain 0.14 |

전부 Mixkit Free License. 얼굴이 식별되는 인물이 없다. 다만 Mixkit은 TV·라디오 방송 사용을
금지하므로 웹·유튜브 배포로 한정한다.

## 걸린 것

**B롤 경로 기준이 두 개다.** 클립의 `video` 는 영상 작업 폴더 기준이라 `../..` 두 단계인데,
`photo.html` 의 `src` 는 템플릿 폴더 기준이라 세 단계다. 한 단계를 덜 올라가 "영상 소재가
없습니다"로 죽었다.

**판 없는 자막이 밝은 B롤에서 묻힌다.** `subtitleBox: false` 는 어두운 카드에서만 통한다.
`photo.html` 에만 있던 `shade` 를 B롤에도 추가해 해결했다(`media.js`).

**`colorlevels` 의 `rimax` 는 어둡게 하는 파라미터가 아니다.** 입력 최대값을 낮추면 범위가
압축되며 **오히려 밝아진다.** 출력 최대값인 `romax` 를 낮춰야 한다. `shade` 는 캐시 키에도
넣어야 한다.

**밝기가 아니라 무늬가 문제인 경우는 `shade` 로 못 고친다.** `ai-code.mp4` 는 어둡게 해도 초록
헥스코드가 자막과 간섭했다. 하단이 매끈한 `ai-datacenter.mp4` 로 교체했다.
**B롤은 자막이 지나가는 하단이 비어 있는지 먼저 본다.**

**`ai-film.mp4` 는 세로에서 못 쓴다.** 9:16 크롭 3초 지점이 초점이 나가 있다.

**프레임 몇 장으로는 못 잡는 것이 있다.** 나는 t=2·6·14 프레임만 보고 "깨끗하다"고 판단했는데,
평가 agent 가 t=26.5 이후를 뽑아 **outro CTA 와 자막이 겹쳐 마지막 4초가 판독 불가**인 것을
찾아냈다. 끝 구간을 안 본 것이다. 또 `silencedetect` 로 **문장 사이 무음이 1.9~2.0초씩 7번**
있다는 것도 잡아냈다 — 프레임으로는 절대 안 보이는 결함이다. 검증할 때 오디오 타임라인도 재야 한다.

**`chapter.html` 은 번호가 강제로 붙는다.** `auto` 클립에 num "3" 을 넣었더니 "자막" 옆에 큰
숫자로 붙어 "자막 3종"으로 읽힌다. 대본에서 근거 없는 수치를 다 뺐는데 템플릿이 숫자를 하나
더 만들어 넣은 셈이다.

## 남은 것

평가에서 나온 "고칠 것" 6가지가 그대로 남아 있다. 특히 1~3번(CTA 겹침, 무음 51%, 오독되는 "3")은
다음 렌더에서 고쳐야 한다. B롤 3종이 1280×720이라 세로에서 업스케일되는 것도 남았다.

파이프라인은 **B롤 위에 텍스트 카드를 얹지 못한다**(`compose.js` — 클립은 `video:` 아니면
`file:` 하나). "영상미 있는 화면 + 그 위의 카피"라는 흔한 쇼츠 문법을 이 저장소는 못 쓴다.

## 평가

*2026-08-08 · video-reviewer*

| 항목 | 점수 | 근거 |
|---|---|---|
| technical | 5 | H.264 High/yuv420p/AAC-LC 48kHz, 1080x1920 30fps, 30.03s. moov가 mdat 앞(faststart 있음). I=-14.4 LUFS, True peak -1.3 dBFS, LRA 3.9 LU — 목표 -14 LUFS에 0.4 이내 |
| visual | 2 | t=26.2s부터 끝까지 outro CTA 알약 버튼과 소각된 자막이 겹쳐 글자가 서로 뭉개진다(review-t26.5·t27.5·t29.5.png). 마지막 3.8초, 즉 CTA를 읽어야 할 구간 전체가 판독 불가 |
| audio | 2 | 발음·속도는 정확하나 30.03초 중 실제 발화가 15.3초(51%)뿐. 문장 사이 무음이 일관되게 1.9~2.0초씩 7번(silencedetect -40dB). 쇼츠에서 절반이 정적이면 이탈 구간이 된다 |
| narrative | 4 | 훅(0개) → 자기증명 → 구성요소 → 마무리 순서는 말이 되고, 30초 안에 "대본만 쓰면 된다"가 전달된다. 다만 close 나레이션이 워터마크 문구와 같은 문장이라 마지막이 동어반복이다 |
| subtitle | 4 | 7개 큐 모두 scenes.pipeline.json의 narration과 글자 단위로 일치. 오타·끊김 없음. MarginV 355px로 유튜브 하단 UI 330px를 25px 차이로 겨우 비켜간다 — 여유가 없다 |
| fitness | 3 | 파이프라인이 무엇을 자동화하는지는 전달된다. 그러나 CTA("develop-video")가 겹침으로 안 읽혀 시청자가 다음에 뭘 할지 모른 채 끝난다. 홍보 쇼츠의 목적을 절반만 달성 |

**overall: 3.3**

**고칠 것 (중요한 순서로)**

1. **outro의 CTA와 자막 충돌을 없앤다** — close 클립(t=26.2~30.0s)에서 CTA 알약이 자막과 같은 y대역에 온다. `outro.html`의 cta 블록을 위로 올리거나, close 클립 구간만 자막을 끄거나(`cta` 문구가 이미 화면에 있으므로 중복), 이 클립에 한해 `subtitleMargin`을 키운다. 지금 상태로는 마지막 4초가 못 읽는 화면이다.
2. **문장 사이 무음 1.9~2.0초를 0.5초 이하로 줄인다** — 클립 길이가 발화(~1.9s)의 두 배 이상(3.3~4.9s)으로 잡혀 생긴 간격이다. 각 clip 길이를 나레이션 + 0.4s 여백으로 맞추면 같은 대본으로 20초 안에 끝나거나, 남는 10초에 내용을 더 넣을 수 있다.
3. **`auto` 클립의 "3"을 없앤다** — chapter.html의 챕터 번호가 "자막 / 내레이션에서 자동 생성" 옆에 큰 숫자로 붙어 "자막 3종"으로 오독된다(review-t23.png). 대본이 "검증된 숫자는 0과 7뿐"이라 못박았는데 화면에 근거 없는 세 번째 숫자가 나온다. stat 계열이 아닌 템플릿을 쓰거나 num을 비운다.
4. **밝은 B롤 위 워터마크 대비를 올린다** — Mark 스타일이 `&H1AFFFFFF`(약 90% 투명)라 t=5.5s 금색 ai-create.mp4 위에서 거의 사라진다(review-t5.5.png). 어두운 배경(t=2)에서는 읽힌다. 불투명도를 올리거나 얇은 그림자를 준다.
5. **자막 하단 여백을 넉넉히 한다** — `subtitleMargin` 0.185(355px)는 유튜브 쇼츠 하단 UI 330px를 25px 차이로 비켜간다. 기기·UI 변화에 취약하니 0.21(≈400px) 정도로 올린다.
6. **마무리 문구 중복을 푼다** — 워터마크가 30초 내내 "대본만 쓰면 된다"인데 마지막 나레이션과 outro 타이틀이 같은 문장이다. 셋 중 하나는 다른 말로 바꾼다.

**본 것** — 프레임 11장(t=2, 5.5, 9.5, 14, 18.5, 23, 25.5, 26.5, 27.5, 29.5) · ffprobe(코덱/해상도/길이) · mp4 박스 순서(faststart) · ebur128 · silencedetect · 9x16.srt ↔ scenes.pipeline.json 대조 · 9x16.ass 스타일 마진 · motion 클립별 길이
